
+ {ggmosaic} broke with {ggplot2} version 4.0.0
  so this directory contains the images
  from position.qmd in various forms
  (based on {ggplot2} version 3.5.2)

  fig-spine
  fig-spine-weak
  fig-mosaic

  + "ggplot" object

    ./ggplot.rds

  + "gTree" object (grid.force()d and gid.grab()ed from "ggplot" object

    ./gtree.rds

  + SVG and PDF versions (width=8, height=4)

    ./fig-ternary.svg
    ./fig-ternary.pdf


